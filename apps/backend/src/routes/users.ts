// apps/backend/src/routes/users.ts
import express from 'express';

const router: express.Router = express.Router();

// NOTE: Admin-only user management endpoints have been moved to /api/admin/users
// Public/user-facing endpoints can be added here (e.g., profile, update own details).

export default router;